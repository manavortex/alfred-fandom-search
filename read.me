# initial setup
call `fandom set <wikiname>` so the workflow knows where to search.  
The wiki name is the one in the url, e.g. "*lyrics*.fandom.com"

after, find articles with `fandom <query>`. Return opens in browser, cmd+return copies to clipboard. 